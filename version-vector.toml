# Three-layer version vector (RFC-003: L4 shelld retired).  THE
# marspot version is the L2 (core) version — that's what the title
# bar displays and what users mean when they ask "what version of
# marspot are you on?". L1 and L3 each carry their own semver, bumped
# only when their specific code surface changes.
#
# Canonical layer numbering (used everywhere — code, logs, docs):
#
#   L1 = marspot-shell    NSWindow owner / supervisor state machine /
#                         banner / probation logic / shell self-update
#                         path. Stable wrapper; bump rarely.
#
#   L2 = marspot-core     THE marspot version. Renderer (Metal), UI
#                         dispatch, layout, session bootstrap, control-
#                         socket protocol driving, registry-based L3
#                         spawn + reattach (RFC-003). Bump on every
#                         user-visible release.
#
#   L3 = marspot-session  Per-pane terminal engine + PTY owner: parser,
#                         grid, scrollback, shm publish, UDS control
#                         surface, bytelog.  Bump when in-process
#                         terminal mechanics or the L2↔L3 wire change.
#
# Bump protocol: bump the version below, then add ONE entry to the
# matching section in CHANGELOG.md (newest on top).  The trailing `#`
# subject here is the one-line summary; full why/how lives in the
# commit message and in CHANGELOG.md.  Do NOT keep appending to this
# line — that grew to ~25 KB before and ate `git diff` readability.

shell   = "0.7.7"  # cc @model 切 profile 后不再显示旧模型 — 按 claude pid 变更设读取围栏.前 0.7.6 共享 crate(layout 第 5 钮)重链接.前 0.7.5 DevPanel 冷启动永远隐藏 — 不再恢复持久化的 visible(一次调试开过就每次自启的烦恼);位置尺寸照旧恢复,toolbar 第 4 icon 召唤.前 0.7.1 snapshot v5 重链接.前 0.7.0 RFC-004 D.1 — L1 入口跑 migrate_legacy_state_root(Caches → Application Support,rename+symlink 一次性迁移).前 0.6.88 笔记见 git blame.
core    = "0.12.31"  # 交通灯 hover 显 ×/−/+ 图标,跟系统一致(悬停标题栏三个都显,不是单个).前 0.12.30 Process Monitor 的交通灯原来是第三份手写副本(自带常量+自带描边),前两轮改的组件它压根不调用;现在共用唯一出处.前 0.12.29 交通灯去掉内侧描边 —— 它是 alpha 修复后才显形的,从内侧啃掉每边 1px;实测原生 14px 我们只剩 9px.前 0.12.28 Process Monitor 四边补内边距(内容不再贴边/溢出)+ 绘制与热区共用同一套几何常量.前 0.12.27 主循环看门狗 — 冻结进行时就报告,不必等迭代结束.前 0.12.26 低优先级收尾 — L2 停顿阈值改具名常量、attach 分支 phase 改名避免同迭代重名、spawn 死参数删除、失败路也走 adopt_backend、交通灯测试从常量推导.前 0.12.25 cc modal 几何独立成 ui/components/cc_usage_modal(具名 metric + 卡高推导 + 面板 rect),painter 只管画 —— 与 layout_modal 的分层惯例对齐.前 0.12.24 ShapeCache 文档归位;共享 crate(握手超时受 deadline 约束、bytelog 恢复工具跨轮转段)重链接.前 0.12.23 四个 writer 收敛到一个 BoundedWriter<T> + SnapshotWriter 独立成模块.前 0.12.22 队列容量四处收成一处(frame_writer::cap)+ 快照不再为打日志白拷 100-300KB.前 0.12.21 pending 槽加超时逃生口(worker 丢了不再锁死复活路径)+ 补 pane/LRU 回归测试(旧的分辨不出 LRU 与 FIFO)+ font_cache 文档与字段名对齐实现.前 0.12.20 自审收口 — 修重连后 Cmd-C 静默失效(swap_control 漏换 selection_rx)+ 常量文档串位 + 删测试专用包装 + L3 停顿探测器拆细 phase + L2 写失败样板 6→1.前 0.12.19 交通灯 14pt 追平原生 + mouse-tracking TUI 滚动清选区(claudecode 里滚一下选区不再罩错内容).前 0.12.18 cc modal 两个 section header 改用系统 UI 字体(SF Pro 系统字号),与原生 header 同尺寸,不再是终端等宽小一号;ViewPainter 补 ui_text 比例字体原语.前 0.12.17 cc 卡片四边同一 padding + 卡高由内容推导 + chip 文字按 ink 垂直居中.前 0.12.16 cc modal 版式放宽(卡片内边距 / 两段间距 / OK 与百分比右对齐)+ 建 pane 搬出主循环 + wait_and_connect 调用方预算显式化.前 0.12.15 阻塞面收口 — L2→L3 每条控制流走 FrameWriter(一个卡住的 L3 不再锁死整个 L2)+ 阴影层预乘修正 + wait_and_connect 单一 deadline(原来是 2×).前 0.12.14 cc 时间轴右侧留标签槽 — 窗口条的 tag 不再压在 modal 边框上;轴按最宽 tag 收窄,面板同步加宽补回分辨率.前 0.12.13 ui_rect 混合因子修正(着色器输出预乘,源因子却是 SourceAlpha,alpha 被乘两遍 —— 半透明填充一直不可见)+ 主循环阶段名细分.前 0.12.12 cc 时间轴日期改虚线贯穿图表 — 日期不再只在轴下当标签,每天一条淡虚线上到图里,条的端点可以就地对着日期读;NOW 保持实线做对比.前 0.12.11 L2 主循环根治阻塞 — 重连搬线程 / 写 L1 走 FrameWriter / ShapeCache 计数戳 LRU / cwd 负缓存 / .next_id 非阻塞 flock / font_cache 逐条驱逐 + l2.loop.stall 停顿探测.前 0.12.10 Cmd+Shift+C 开关 Cc usage modal(排在 Cmd-C 复制之前,否则被当成复制吞掉;也排在 LOCK_KEYS 之前,claudecode 占键盘时照样能开).前 0.12.9 Cc modal 视觉重排(对比度分层 + 全宽条 + 状态 chip)+ 滚轮单事件 tick 上限.前 0.12.8 Cc modal 状态语义化 + 卡片保留槽布局 — allowed_warning 等原始 header 值不再直出;身份行按预留槽排(状态右对齐→名字→email 按余量截断),任何长度都不重叠.前 0.12.7 Cc 钮换矢量 icon(UsageBarsIcon,基线+三段升柱)— 文字标签在四个描边 icon 中是异类.前 0.12.6 cc usage modal 视觉修正 — 条高 0.55ch + 调色接 ui token + 时间轴按用量填充(轨道=窗口区间,彩条=已用部分).前 0.12.5 cc usage modal — 工具栏 Cc 钮开 Claude 账户用量面板:n 账户卡片(5H/7D 利用率条 + reset/updated)+ ±6 天资源可用时间轴(5h/7d 窗口条 + NOW 线 + 日刻度);数据源 ~/.local/state/devops/claude-usage.json,自写零依赖解析,开着才读(5s 刷新),关闭零 IO.前 0.12.4 marspot-linkify 石头孵化 — 特征值识别(URL/路径/email/IP/UUID)提取为零依赖独立 crate,CellSource trait 为唯一输入面;grid_links 变薄 adapter,调用面不变.前 0.12.1 snapshot v5 重链接.前 0.12.0 RFC-004 装配身份制全量 — pane 永不漂移永不压缩:按格 sid 装配(活→reattach/死→同 id 复活/失败→Vacant 占位)、身份验证判活(pid 复用不再误杀)、reattach 失败降级复活不删目录、孤儿收养、回收站 GC(retired/ 14 天)、标题按 sid 绑定、[+] cap 统一 36、C.2 连接重试面扩大、状态根迁移.前 0.11.62 笔记见 git blame.
session = "0.11.19"  # 主循环看门狗 + 共享 crate 重链接.前 0.11.18 共享 crate(StallReport::summary)重链接.前 0.11.17 共享 crate(握手超时受 deadline 约束)重链接.前 0.11.16 PtyWriter 退化成 BoundedWriter 特化 + SnapshotWriter 独立成模块.前 0.11.15 快照写盘不再白拷一份 body 只为打日志;队列容量改引用共享 cap.前 0.11.14 共享 crate(uds_session_client 单一 deadline)重链接.前 0.11.13 周期快照的写盘搬出主循环 — 现场实测 115KB 快照在磁盘忙时写了 2.87 秒,整条循环冻住.前 0.11.12 FrameWriter 提到共享 crate(ControlWriter 改为薄封装)+ .next_id 非阻塞 flock 重链接.前 0.11.11 控制 socket 写搬出主循环(实测 macOS AF_UNIX SO_SNDBUF 仅 8192,L2 一停读 L3 主循环即钉死)+ 主循环停顿探测器 l3.loop.stall.前 0.11.10 bytelog 轮转取代拷贝压缩(共享 crate 重链接)— 越过 100MB 上限时不再在主循环上拷 50MB,现场实测冻住 pane 10 秒以上;改为一次 rename.前 0.11.9 PTY 写路径搬出主循环 — raw 模式子进程不读 stdin 时 write(2) 阻塞整条 L3 主循环(单 pane 卡输入数分钟后自愈的根因);改为专用 writer 线程 + 256KB 有界队列 + 短写补齐.前 0.11.8 共享 crate(layout)重链接.前 0.11.7 linkify 孵化重链接.前 0.11.4 CSI 3J 清到持久层 — clear 后重启不再复活历史;hot pair 截断 + cold 删除 + mmap 失效,bytelog 不动(重放会重现同一 3J).前 0.11.3 v5 live 验证轮 — execv 场景 alt 屏原样恢复(fold 语义误用导致输入框消失/重绘慢的现场回归修复)+ execv 恢复后 SIGWINCH 前台进程组兜底.前 0.11.1 C.1 快照升 INFO — session 目录 flock(A.3)、周期快照 30s 防抖(C.1,硬宕机窗口有界)、快照 v4 alt 折叠(B13)+ B14 tail 序反修复、scrollback 损坏隔离重建(A.4)、.next_id 自愈(A.1)、Drop 去毁灭化.前 0.10.21 笔记见 git blame.
