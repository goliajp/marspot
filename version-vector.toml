# Three-layer version vector (RFC-003: L4 shelld retired).  THE
# marspot version is the L2 (core) version — that's what the title
# bar displays and what users mean when they ask "what version of
# marspot are you on?". L1 and L3 each carry their own semver, bumped
# only when their specific code surface changes.
#
# Canonical layer numbering (used everywhere — code, logs, docs):
#
#   L1 = marspot-shell    NSWindow owner / supervisor state machine /
#                         banner / probation logic / shell self-update
#                         path. Stable wrapper; bump rarely.
#
#   L2 = marspot-core     THE marspot version. Renderer (Metal), UI
#                         dispatch, layout, session bootstrap, control-
#                         socket protocol driving, registry-based L3
#                         spawn + reattach (RFC-003). Bump on every
#                         user-visible release.
#
#   L3 = marspot-session  Per-pane terminal engine + PTY owner: parser,
#                         grid, scrollback, shm publish, UDS control
#                         surface, bytelog.  Bump when in-process
#                         terminal mechanics or the L2↔L3 wire change.
#
# Bump protocol: bump the version below, then add ONE entry to the
# matching section in CHANGELOG.md (newest on top).  The trailing `#`
# subject here is the one-line summary; full why/how lives in the
# commit message and in CHANGELOG.md.  Do NOT keep appending to this
# line — that grew to ~25 KB before and ate `git diff` readability.

shell   = "0.6.47"  # DevPanel chrome SF Pro 启用 + 字号 0.85x.之前 shell 一直 0.6.46 没 bump,8 round DevPanel hierarchy rewrite 全编进 lib 但 install-local 检测 shell binary 内 LAYER_VERS 未变就 skip stage,L1 一直跑老 binary,user 打开 dev panel 看到的还是 rewrite 前的旧菜单.本轮 bump 让 L1 真换新 binary + 改两条 dev panel chrome 默认 font path:1) `dev_window.rs:render_canvas_into_layer` ui_font=false → true 让 view-tree default 走 SF Pro shape 路径(Foundation/Tokens/Primitives/Components/Architecture/Build Identity 等 hierarchy 内容用 proportional);2) chrome_cell_w/h/ascent 全乘 `DEV_PANEL_FONT_SCALE = 0.85` ≈ 11pt 视觉(原 13pt 太大,信息密度高的 menu + content 一屏装更多).legacy `draw_*_sample`(Colors/Units/Rects/Lines/Text)走 mono Monaco 直 emit path,不受 ui_font 影响 — visual mix 但不冲突.前 0.6.46 笔记保留见 git blame. — view layout 用真实字体 metrics.正确架构:`view::TextFontSpec` (Mono { size } / Ui { size_q, weight, opts_bits }),`View::Text` 加 `font` 字段(默认 Mono);`Text::ui(pt, weight, opts)` 链式 API 切 SF Pro path.新 `FontMetricsProvider` trait + `MockFontMetrics`;`LayoutCtx<'a>` 加 `fonts: &'a dyn FontMetricsProvider`,View::Text 分支用 `ctx.fonts.advance_phys` / `line_h_phys` 拿真实尺寸.font_shape 加 `measure_line(text, font, opts) -> f64` 经 CTLine typographic-bounds.FontCache 加 `measure_ui_text(text, weight, opts) -> f64` + `ui_line_h_phys()`.新 `src/chrome_measure.rs::ChromeMeasure<'a>` 用 RefCell<&'a mut FontCache> 实现 trait —— Mono → cell metrics,Ui → 真 SF Pro shape.paint 把 view::Text.font 映射到 canvas TextPrim 已有 font_kind/weight/opts.dev_window + render_metal 4 处 build_dev_panel_canvas caller 同步 pass `ChromeMeasure::new(font_mut, w, h)`.`build_font_v5_view()` 返完整 view 树(vstack + hstack + Text.ui()),`draw_font_v5_sample` 退役.snapshot test size 改 production default 420×520 (840×1040 px).全 246 lib 测试 PASS.
core    = "0.11.26"  # 跟 shell 0.6.47 同 commit ship — DevPanel chrome SF Pro 启用 + 字号 0.85x(详见 shell 笔记).Architecture SSIM baseline 已锁定为 Monaco mono 13pt 视觉,SF Pro + 字号缩小后 SSIM 会跌(unlock 后续 round 重 lock baseline).前 0.11.25 Architecture SSIM 笔记保留见 git blame.  # DevPanel 层级化 round 11 — 加 `devpanel_architecture_snapshot` SSIM baseline 把 round 10 ship 的 Sessions > Architecture 视觉锁住.840×1040 PNG @ 2× retina,active_tab=TAB_SESSIONS + active_section=SECTION_SESSIONS_ARCHITECTURE.内容全 static(RFC-003 process tree 描述 / RFC-001 plugin list / hard caps 数字 / wire protocol 描述)所以 SSIM gate 真有意义 — Build Identity 因含 git_sha + build_ts 不能 SSIM 但 Architecture 可以.覆盖契约:Sessions tab active 状态(blue underline)/ Architecture item active highlight(blue BG)/ menu 上下 SubGroup "Overview" + "Live state(待 wire)" header 视觉 / right content 4 段 vstack(Process tree / Plugins / Hard caps / Wire protocol).bin/font-snapshot-check.sh 升 13 个 baseline.Phase 9 之上多一个 dev panel sub-tree SSIM 守(showcase / components_catalog / typography 三个 UI subset + architecture 总共 covers UI 4 subset 的 3 个 + Sessions 1 个 subset).baseline SSIM 1.0000 PASS @ 0.98.前 0.11.24 Sessions Architecture Item 笔记保留见 git blame.  # DevPanel 层级化 round 10 — Sessions Tab 加 Architecture Item.之前 Sessions Tab 空 placeholder,本轮加第一个 populated Item(类似 round 8 Render > Build Identity 套路,纯 static / env 数据无 backend wiring).新 `SECTION_SESSIONS_ARCHITECTURE = 0x03_01_01`.`build_architecture_view()` 4 段 vstack(Process tree / Plugins / Hard caps / Wire protocol):RFC-003 三 process(L1 shell / L2 core / L3 session × N)+ "L4 shelld 退役" 注解;RFC-001 两 plugin(claudecode + pidtree)+ static-link note;Hard caps(Max sessions 9 / 80x24 / Scrollback Memory 10k vs File 不限);Wire(L1↔L2 shell-proto / L2↔L3 UDS + shm + bytelog).TextStyle BODY + weight Bold 替代不存在的 BODY_BOLD.2 个 placeholder test 重写用 `usize::MAX` synthetic empty tab id(real TAB 都 populated 后,不依赖某个 TAB 当前是否空)— `empty_tab_renders_placeholder` 替 `non_ui_tab_renders_placeholder`,`hit_test_skips_menu_on_empty_tab` 同改.636/636 lib tests PASS.前 0.11.23 stale doc 清理笔记保留见 git blame.  # DevPanel 8 round rewrite 后残留 stale doc 注释清:`DevPanelState.active_tab` doc 还提 `TAB_TOKENS / TAB_COMPONENTS`(round 1 已删)改成现 `TAB_UI / TAB_FONT / TAB_RENDER / TAB_SESSIONS` 准确列;`DevPanelState.active_section` doc 还提 `SECTION_COLORS` 默认 + "stacks ALL sections regardless"(老行为,现 default = SECTION_UI_FOUNDATION_MODEL,dispatch 单 section)改成新编码说明 `(tab << 16) | (subgroup << 8) | item`;`Phase 10c — Font v5 showcase` 注释错位粘到 `build_typography_view` 头(round 3 copy-paste 残留)删,只留正确 Typography doc;Model section content 标题从 "v3 Model — overview"(rewrite 前 v3 标签)改成 "Model — UI system overview" 反映 hierarchy 重写后的语义.636/636 lib tests PASS + 12/12 SSIM PASS(stale doc 改不影响任何 snapshot baseline 视觉).前 0.11.22 Build Identity Item 笔记保留见 git blame.  # DevPanel 层级化 round 8 — Render Tab 加 Build Identity Item.之前 Render Tab 空 placeholder,本轮加第一个 populated Item.frame timing / instance counts / atlas usage 等需 renderer state pipe,继续 placeholder("Render(待 wire)" SubGroup header).Build Identity 全部数据从 env 拿(`build.rs` 烤的 MARSPOT_VERSION_SHELL/CORE/SESSION + MARSPOT_GIT_SHA + MARSPOT_BUILD_TS + cfg!(debug_assertions))+ runtime MARSPOT_STATE_DIR / MARSPOT_SESSION_ID 两条,无 backend wiring 需求.三段 vstack(Layer versions / Build provenance / Runtime)+ 末注解释 install-local.sh 把 git_sha + build_ts 写进 rodata 作 fingerprint.新 `SECTION_RENDER_BUILD_IDENTITY = 0x02_01_01`(tab=Render / subgroup=1 / item=1).非 UI tab placeholder test 改用 TAB_SESSIONS(TAB_RENDER 现有 Item 不再 fall back).636/636 lib tests PASS.前 0.11.21 Phase 9 12/12 acceptance 笔记保留见 git blame.  # DevPanel 层级化 round 7 — 加 `devpanel_typography_snapshot` SSIM baseline 把 round 3 ship 的 Typography Item 视觉锁住.840×1040 PNG @ 2× retina,active_tab=TAB_UI + active_section=SECTION_UI_TOKENS_TYPOGRAPHY.覆盖:hierarchy menu(Typography Item active 蓝 highlight)+ 三段 right content:Size scale(Caption/Body/Header/LargeHeader 四行同字 sample,垂直扫看大小阶差)+ Weight(Regular/Bold 两行)+ 8 named TextStyle tokens(CAPTION / BODY / HEADER / LARGE_HEADER / HINT / CODE / LINK 蓝 / ERROR 红).契约抓 silent regression in Monaco mono advance / line-height / cap-height ratio / theme token color drift.bin/font-snapshot-check.sh 12 个 baseline 全 SSIM 1.0000 PASS @ 0.98.Phase 9 matrix 现 12/12 baseline 全锁(原 design doc 矩阵目标达成).前 0.11.20 Components Catalog baseline 笔记保留见 git blame.  # DevPanel 层级化 round 6 — 加 `devpanel_components_catalog_snapshot` SSIM baseline 把 round 4 ship 的 Components Catalog 视觉锁住.840×1040 PNG @ 2× retina,active_tab=TAB_UI + active_section=SECTION_UI_COMPONENTS_CATALOG.覆盖 hierarchy 完整 menu 渲染(Foundation / Tokens / Primitives / Components 4 SubGroup × 14 Item)+ Component Catalog 右侧 4 group:Status badges(Success/Warning/Danger/Accent pill 4 色)/ Interactive(toggle off / on / picker)/ Containers(card / panel / tooltip)/ Lists & navigation(部分可见).契约抓 silent regression in:badge pill 圆角 / shadow blur / border color drift / glyph baseline 漂 / corner radius scale 错位 / shadow 跟 border 互相覆盖.bin/font-snapshot-check.sh 加 11/12.baseline SSIM 1.0000 PASS @ 0.98.前 0.11.19 Font > Baselines Item 笔记保留见 git blame.  # DevPanel 层级化 round 5 — Font Tab 加 Baselines Item.Font Tab 之前只一 Item(Showcase),加 SubGroup "Visual gates" + Item "Baselines" 让 SSIM 矩阵在 panel 内可视化.新 `SECTION_FONT_BASELINES = 0x01_02_01`(tab=Font / subgroup=2 / item=1).`build_baselines_view()` 三列 row(name × dims × coverage tag,10 baselines + "threshold ≥ 0.98" header + workflow hint 末注):font_v5_showcase(840×1040 chrome SF Pro Font v5 panel)/ mono_grid(1200×600 PTY mono)/ box_drawing(1000×500 Monaco raster)/ subpx_fingerprint(1000×500 Phase 4)/ chrome_small_sizes(1200×600 SF Pro 11-14pt + var weight)/ cjk_fallback_baseline(1400×600 SF Pro CJK cascade)/ emoji_color(1200×600 Phase 7)/ opentype_opts(1400×900 Phase 8)/ terminal_scene(1200×600 终端场景)/ chrome_decoration(1400×500 shadow + border).hardcoded mirror 不 fs scan,新 baseline 加这里 + snapshot test + bin/font-snapshot-check.sh 三处.Font Tab 现 2 Items(Showcase + Baselines),2 SubGroup(Font v5 + Visual gates).634/634 lib tests PASS.前 0.11.18 Components Catalog 笔记保留见 git blame.  # DevPanel 层级化 round 4 — Components SubGroup 补 Catalog Item.UI Tab 4 个 SubGroup(Foundation / Tokens / Primitives / Components)首次全 populated.新 `SECTION_UI_COMPONENTS_CATALOG` 常量 + menu row + content dispatch.`build_components_catalog_view()` 4 group / 14 row hstack(label + 实例)集中 showcase:Status badges(Success / Warning / Danger / Accent 4 个 badge)/ Interactive(toggle off+on / picker)/ Containers(card / panel / tooltip)/ Lists & navigation(list_row idle + selected / context_menu with divider / breadcrumb).label + sample center-aligned hstack 行,group_header 用 HEADER style.稳定 ViewId 0xDE7_C000-0xDE7_C0FF 范围给互动 widget(避免跟 Foundation / Tokens / Primitives 撞).634/634 lib tests PASS.前 0.11.17 Tokens > Typography 笔记保留见 git blame.  # DevPanel 层级化 round 3 — Tokens SubGroup 加 Typography Item.之前 Tokens 下只有 Colors + Units,Typography 占位但没内容(Text item 是颜色 demo,不是型号阶 catalog).加 `SECTION_UI_TOKENS_TYPOGRAPHY` 常量 + menu row + content dispatch.新 `build_typography_view()` 三段 view-tree(vstack of vstacks):1) Size scale — Caption/Body/Header/LargeHeader 每行 left label + right "The quick brown fox jumps" 同字 sample,vertical scan 比较大小差;2) Weight — Regular/Bold 两行同 body size;3) Style tokens(`theme::text::*`)— 8 个 named style(CAPTION / BODY / HEADER / LARGE_HEADER / HINT / CODE / LINK / ERROR)各一行,看具体 sample 文字渲染.行间 align_cross_end 对齐 baseline,label 用 CAPTION 灰 mute fg,sample 用对应 token style 完整应用.634/634 lib tests PASS.前 0.11.16 alias 全替 + dispatch 数据驱动笔记保留见 git blame.  # DevPanel 层级化 round 2 — 旧 SECTION_* alias 全替为新名字(SECTION_UI_FOUNDATION_MODEL/L1-L6 / SECTION_UI_TOKENS_COLORS+UNITS / SECTION_UI_PRIMITIVES_RECTS+LINES+TEXT / SECTION_FONT_V5_SHOWCASE),删 13 个 back-compat alias 让常量名跟层级一致.snapshot test `font_v5_showcase_snapshot` 改 `active_tab=TAB_FONT` + active section 新名字,menu + content 现在都对齐显示 Font tab 的 "Font v5 > Showcase".dispatch gate 从硬编码 `active_tab != TAB_UI` 改为数据驱动:扫 TAB_MENUS 找 active tab 是否有 Item,无 Item(Render / Sessions)→ placeholder;有 Item(UI / Font)→ menu + content.hit_test 同样去掉 TAB_UI 锁,数据驱动从 TAB_MENUS 取菜单 — Header 仍非命中,Item 命中,empty tab 全 None.snapshot 视觉确认 Font tab 完整 menu("Font v5" Header + "Showcase" active Item)+ content(Phase 5/7/8 panels)对齐.634/634 lib tests PASS.前 0.11.15 hierarchy round 1 笔记保留见 git blame.  # DevPanel 重命名 + 严格层级化 round 1 — 之前叫 "UI DevPanel" 但 UI 只是 dev tool 的一部分;`Tokens` / `Components` 之前是平级 top Tab 但二者都是 UI 系统内部产物.重写为 3-level 严格层级化:DevPanel(root)→ Tab(top horizontal,UI / Font / Render / Sessions)→ SubGroup(left menu 标签头)→ Item(可点入口).新 enum `MenuRow { Header / Item }` 数据驱动 menu render + hit_test;新常量编码 `SECTION_xx = (tab << 16) \| (subgroup << 8) \| item` 三层强类型(SECTION_UI_FOUNDATION_MODEL / SECTION_UI_TOKENS_COLORS / SECTION_FONT_V5_SHOWCASE 等).TAB_TOKENS / TAB_COMPONENTS 删,Font v5 从 UI items 提到独立 TAB_FONT.UI Tab 内 4 SubGroup:Foundation(Model + L1-L6)/ Tokens(Colors + Units)/ Primitives(Rects + Lines + Text)/ Components(预留).新 token `MENU_HEADER_FG`(FG_MUTED)让 Header 视觉与 Item 分层.旧 `SECTION_*` 名字保留为 alias 不破 caller(下轮 review 后删).4 tests 更新:non_ui_tab_renders_placeholder → 用 TAB_RENDER;hit_test_lands_on_tab_strip → 2nd tab 现 TAB_FONT;hit_test_lands_on_menu_row → 跨 Header offset 调整;hit_test_skips_menu_header_rows + skips_menu_on_empty_tab 新加.634/634 lib tests PASS.前 0.11.14 workspace warnings 0 笔记保留见 git blame.  # workspace warnings 18 → 0 全清(纯 polish 无 hot path / wire 改).render_metal.rs:删 `use objc2::msg_send`(retired),prefix 4 个 unused params(`dots` / `tab_h` / `ui_rects` / shadowed `Cell`)`_`,去 unnecessary `mut` on `bump` closure.dev_window.rs:4 个 `unnecessary unsafe` 块改干净 expr (`setLevel` / `backingScaleFactor` × 2 / `setFrame_display`)— objc2 升级后变 safe;1 个 unused doc comment(`/// thread_local!`)改 `// `(rustdoc 不会处理 macro invocation 的 doc).ui/view/view.rs:删 `space` 未用 import × 2(context_menu / list_row).ui/view/lazy.rs:删 `Modifier` 未用.marspot-core.rs:`sorted_to_orig = orig.clone()` 改 `clone_from(&orig)` 消 unused_assignments + 顺手避一次 alloc.claudecode.rs:`client.detach_raw(sid)` 加 `let _ =` 显式忽略 Result(注释:fn 内是 best-effort cleanup,失败无 user-visible 影响).633/633 lib tests PASS.前 0.11.13 bench.sh SSIM gate 笔记保留见 git blame.  # bench.sh 接 Phase 9 SSIM gate — `--ssim` 显式开,`--full` 隐式带(pre-merge gate 位置).--update-baseline write-mode 不带(那是开发者 lock baseline 时跑).script 末尾在 Python perf gate 之后加 SSIM 链:`set +e` 包 python 让 perf fail 不 abort,捕获 PERF_RC,WITH_SSIM 时跑 font-snapshot-check.sh 捕获 SSIM_RC,最后联合 exit `(PERF_RC \|\| SSIM_RC)`.smoke test:本地 fast bench(debug binary)perf GATE FAILED 10/11(本机数据按 [[feedback-bench-mini-only]] 不算数)+ SSIM GATE PASSED 10/10 全 1.0000 — 链路对接正确.--help 更新:--full 显式说"AND the Phase 9 SSIM gate",--ssim 单独标准 mode 说明.10/12 Phase 9 baseline 现在被 mini bench gate 一起守(下次 `bin/bench-remote.sh` 跑会同时跑 SSIM check).前 0.11.12 chrome decoration baseline 笔记保留见 git blame.  # Phase 9 baseline 第十号 — `font_v5_chrome_decoration_snapshot`(1400×500 PNG)锁 F3+1.7 修复的 chrome 装饰复合 path(rounded rect + drop shadow + border).4 列对比一目了然:1) radius 单独(无 shadow)2) +shadow blur=12 offset=(0,4) 标准 modal 3) +border 1pt(完整 F3+1.7 modal frame)4) depth stack 三层 rect 渐进 shadow blur (modal-on-modal / popover-on-popover).契约抓:shadow blur clamping / radius corner drift / border 跟 shadow 互不覆盖 / 多层 stack 阴影叠加正确.baseline SSIM 1.0000 PASS @ 0.98.10/12 Phase 9 baseline 入库.前 0.11.11 terminal scene 笔记保留见 git blame.  # Phase 9 baseline 第九号 — `font_v5_terminal_scene_snapshot`(1200×600 PNG)锁 PTY 终端场景 cell-rect + glyph 复合管道.production PTY render 路径(`render_layout_to_texture` + StorageModeShared 读回)需要新基础设施,scope 过大;改用 canvas 模拟同样 primitive:多行 Monaco mono log-style 输出(git status + cargo test 结果)+ 3 行选区蓝色 BG (row 3 起 col 4 / row 4 完整 / row 5 col 0-30,iTerm2 默认风格)+ 末行尾 cursor block(brighter rect).测的是 cell-rect BG 跨多行 + glyph FG 在 BG 之上的复合契约 — 选区边缘字形不溢出 / 不漂 / BG 层级保留.baseline SSIM 1.0000 PASS @ 0.98.真 PTY readback 留 Phase 10 follow-up(需 StorageModeShared 改造).9/12 Phase 9 baseline 入库.前 0.11.10 OpenType opts 笔记保留见 git blame.  # Phase 9 baseline 第八号 — `font_v5_opentype_opts_snapshot`(1400×900 PNG)锁 Phase 8 OpenType per-context 开关.showcase 锁 full vs all_off,这条加 code() 作第三列,且验证 3 个 preset 渲染差异可见(silent collapse 把 3 个 path 合并到同一管道 → SSIM 在多行同步漂).4 source × 3 preset = 12 行:Tax Wax Yes AVA(kerning visible)/ fi fl ffi ffl(liga visible)/ ==> != <== ===(calt contextual alt)/ AVAVAV WAWAWA LTLTLT(wide-kern triple).label 行用 12pt FG_MUTED 标 source 字符串,3 个 preset 用 22pt FG 各占一行.L4 code/all_off 末尾被裁(SSIM gate 锁可见部分,future drift 在 cover 区域一样 catch).baseline SSIM 1.0000 PASS @ 0.98.8/12 Phase 9 baseline 入库.前 0.11.9 emoji color 笔记保留见 git blame.  # Phase 9 baseline 第七号 — `font_v5_emoji_color_snapshot`(1200×600 PNG)锁 Phase 7 color emoji + ZWJ cluster.color path 独立于 alpha mono path(BGRA atlas + `fg_color_pipeline`),回归 = "emoji 变灰色 silhouette"(pre-Phase-7 fallback)或 ZWJ 拆开变 4 个独立字.4 行 axis:L1 base palette(❤️⭐🌈🍕🚀🎉🍎⚡🦄💎 单 codepoint colour)/ L2 ZWJ family(👨‍👩‍👧‍👦 等多 codepoint joined)/ L3 flags(🇯🇵🇰🇷🇺🇸🇨🇳🇬🇧🇩🇪🇫🇷🇮🇹🇧🇷🇮🇳 regional indicator pair)/ L4 mixed alpha + color(Hello 👋 World 🌍 deploy 🚀 the 🏗️ build ✅ atlas 切换压力).baseline SSIM 1.0000 PASS @ 0.98(本机环境 IO 阻塞 build 跑 53min;sibling Spotlight reindex 完成后会快).7/12 Phase 9 baseline 入库.前 0.11.8 CJK fallback baseline 笔记保留见 git blame.  # Phase 9 baseline 第六号 — `font_v5_cjk_fallback_baseline_snapshot`(1400×600 PNG)锁 chrome SF Pro + CJK cascade fallback baseline 对齐.SF Pro 不带 CJK 字形,CT 走 PingFang SC(中)→ Hiragino Sans/Mincho(日)→ Apple SD Gothic Neo(韩)cascade.per-glyph ascent / baseline 差异在大字号最容易 surface 也最容易"看着 wonky".5 行覆盖 36pt Bold(粗大 fallback)/ 24pt Reg + Bold mixed baseline / 18pt 四语种 mix(Hello 你好 こんにちは 안녕)/ Mac 偏好设定 · Mac の環境設定 · Mac 환경설정 三个 region 系统 UI 文案.36pt 顶 ascent ~30pt 需 pad_y=36pt 起头不被裁.baseline SSIM 1.0000 PASS @ 0.98.6/12 Phase 9 baseline 入库.前 0.11.7 chrome small sizes 笔记保留见 git blame.  # Phase 9 baseline 第五号 — `font_v5_chrome_small_sizes_snapshot`(1200×600 PNG)锁 chrome SF Pro 小字号 + variable weight matrix.showcase 锁大字号(TITLE=22 / H=15 / BODY=13 / EMOJI=20),但 chrome tab strip / sidebar / status badge 都在 11-13pt — sub-pixel AA 漂 / hinting drift / Monaco fallback 在 tiny ASCII 出错 都最容易在小字号 surface 用户却最难肉眼察觉.4 列 11/12/13/14 pt 渲染 same ASCII + CJK + ligature pair (`The quick brown fox jumps  你好  fi fl  ⌘C`) 让 vertical scan 抓 cell-pitch / x-height drift;末段 4 行 100/400/700/900 weight @ 12pt 锁 variable-axis cache 一致性(单 variant 漏 cache → SSIM 直接跌).baseline SSIM 1.0000 PASS @ 0.98.5/12 Phase 9 baseline 入库.前 0.11.6 subpx fingerprint 笔记保留见 git blame.  # Phase 9 baseline 第四号 — `font_v5_subpx_fingerprint_snapshot`(1000×500 PNG)锁 Phase 4 sub-pixel positioning(`subpx_x ∈ 0..4` 4-bucket quantisation).每行触发不同子像素压力:L1 vertical-stem 密集 (iiiiii lllll IIIIII !!!!!!) — atlas slot x-shift 错位 = 字间 bleed;L2 wide-kerned 对 (AVAVAV WAWA YoYoYo TaTa fifi) — Tax/AV/WA 等 sub-pixel boundary 经典案例;L3 code 风格 dense punctuation (Code: foo(bar, baz);  !?,.;:  /* x */);L4 letter count gradient (a aa aaa aaaa...) — 每行一个 a count,4 桶 cell-by-cell 覆盖;L5 CJK fullwidth + ASCII mix (你好 世界 こんにちは 안녕 a b c) — cluster boundary bucket 过渡.Mono Monaco 路径,chrome SF Pro subpx 由 font_v5_showcase 内一行覆盖.bin/font-snapshot-check.sh 接 4 baseline,SSIM 全 1.0000 PASS @ 0.98.前 0.11.5 box-drawing baseline 笔记保留见 git blame.  # Phase 9 baseline 第三号 — `font_v5_box_drawing_snapshot`(1000×500 PNG)针对 Monaco 的 box-drawing + block-element 自渲染 path:`box_drawing_arms` / `block_element_rects` 在 chrome cell metrics 漂移时容易出 hairline gap,但用户往往要月级 soak 才发现一次.5 行覆盖:L1 light + rounded + heavy 框角(┌─┬─┐ + ╭─╮ + ┏━┓)L2 frame interior 含 vertical bars,L3 bottom corners(└─┴─┘ + ╰─╯ + ┗━┛),L4 block elements(█▓▒░ ▀▄ ▌▐ ▔▁ ▍▎▏ ▕),L5 light/heavy junction mix(├─┼─┤ ╠═╬═╣ ┝━┿━┥ ┠─╂─┨).`bin/font-snapshot-check.sh` 加入新 test,3/3 SSIM gate PASS @ 1.0000 / 0.9800.前 0.11.4 dead_code 归零笔记保留见 git blame.  # 全 workspace dead_code 警告归零(从 12+ → 0).删 `CoreEvent::PumpShelld` variant + match arm(RFC-003 退役 shelld wake callback,wake 由 L3 reattach + dual-pump 走同 channel).删 `CoreApp::focused_pane_has_cap` private helper(arity 1 caller 早不在).删 `LogicalLine.{first_phys, last_phys}` 字段(physical row 信息已迁 `norm_to_phys`).删 `LocalSession::{master_raw_fd, try_release_for_handoff}`(Amendment 15 fd-vault SUPERSEDED by Amendment 16 L3 self-execv,见 [[project-amendment-15-fd-vault]]).plugins/mod.rs 加 module-level `#![allow(dead_code)]` —— RFC-001 protocol surface(Capabilities 位常量 / PluginMetadata.version / PtyChild fields / EndReason variants / LogLevel::Error / on_pty_bytes default / PaneSessionHost.{pane_count, pane_pty_device, pane_pty_pid_tree, pane_focused})不该警告每个,silent-by-compiler loud-by-RFC.前 0.11.3 SSIM gate 笔记保留见 git blame.  # Phase 9 SSIM gate ship — `docs/font-rendering-design.md` §9 acceptance(visual regression SSIM > 0.98)从 plan 落成 working gate.pure Rust 实现(无新 deps,符合 self-build):8×8 non-overlap 窗口 SSIM(BT.709 luma + standard k1=0.01 / k2=0.03 / L=255 weights),mean across windows.test 端用三态 env 控:不设 → skip(默认 fast),=1 → 写 PNG fixture(开发者 lock baseline),=check → 读 committed baseline + SSIM ≥ 0.98 assert.两个现有 snapshot(font_v5_showcase + font_v5_mono_grid)接 SSIM gate,baseline = 上次 commit 的 PNG;新 wrapper `bin/font-snapshot-check.sh` 跑 check 模式输出 SSIM 行 + PASS/FAIL.workflow:`bin/font-snapshot.sh` 更新 baseline,`bin/font-snapshot-check.sh` CI / pre-merge gate.初次跑 SSIM=1.0000 / 1.0000 PASS(baseline = freshly committed).未来 silent drift 自动被 cargo test 抓.前 0.11.2 Phase 9 mono_grid baseline + install-local L4 cleanup 笔记保留见 git blame.  # 1) Phase 9 (extended) — 新加 `font_v5_mono_grid_snapshot` 单一 PNG fixture 覆盖 PTY Mono path 5 个 acceptance string (per docs/font-rendering-design.md §9):L1 ASCII alphabet (Monaco cell pitch sanity) / L2 CJK 中日韩 fallback (PingFang/Hiragino/AppleSDGothic 级联) / L3 emoji + ZWJ (mono path 走 silhouette 是预期,colour 通过 chrome 路径在 font_v5_showcase 覆盖) / L4 kerning + ligature (Tax fi fl ff ==> != !==) / L5 RTL bidi placeholder。1200×600 px @ 2× retina,solid dark BG,MARSPOT_FONT_SNAPSHOT=1 env-gated 不影响默认 test 速度。SSIM > 0.98 gate 留 Phase 10 follow-up — 此 commit 只 freeze 渲染字节。2) install-local.sh L4 shelld 升级 dead path 清理(L4 由 RFC-003 退役 + install-shelld.sh 早已删,此前 --with-shelld 调用空指针);删 SHELLD_CHANGED probe + WITH_SHELLD var + arg parser + section 7 + L4 shelld version-vector 行,user 既有 scripts 调 --with-shelld 仍接受但 no-op。前 0.11.1 link panel 回归测 + render_metal dead 大清笔记保留见 git blame.  # 1) link panel regression test — user 2026-06-25 反馈 url/path 左右键弹 panel 一度坏过又自愈 "不知道为什么坏";把 build_link_menu_items body 提为 free fn `link_menu_items_for`,加 #[cfg(test)] mod link_menu_tests 4 case 覆盖 LinkKind::{Url,File,Email} → 2/2/0 items + label + action_tag,并配 every_known_linkkind_is_handled 哨兵:加新 LinkKind variant 时 match 强制 compile-time 决策,防 silent dispatch drift.链路全注释串清(AppKit (right|m)ouseDown: → L1 → MsgType wire → L2 CoreApp::mouse_(right_)down → hit_test_link_at_xy → scan_visible_links → link_menu_items_for → ContextMenuState → renderer set_context_menu → ContextMenu paint).下次 silent drift 由 cargo nextest 抓.4/4 binary tests PASS. 2) render_metal.rs dead code 大清:删 push_close_glyphs (54 lines) / push_sidebar (104 lines) / push_process_tree_icon / push_sidebar_icon / push_empty_cells / push_empty_cell_glyphs / push_add_button_glyph / push_rect / push_border / icon_stroke / push_grid_icon = 11 dead fn ~500 行(view-tree migration 后未删的旧 imperative drawing);删 CHROME_BTN_BG / CHROME_BTN_BORDER / CHROME_PANEL_BG / CHROME_OPTION_BG / CHROME_ICON_FG / CHROME_BTN_BG_HOVER = 6 dead constants(同期 drawing palette);删 4 dead PROCESS_PANEL_* 常量(TAB_BG/TAB_FG/KILL_BG/KILL_FG)+ 4 dead PROCESS_PANEL_BODY_PAD_*/INDENT_LOGICAL.render_metal.rs 不再有"is never used"警告(剩 marspot-core 内 dead variants/methods 留下次).624/624 lib tests PASS 不变.无 hot path 行为变化.前 0.11.0 F2 cleanup 笔记保留见 git blame. — F1 ship 06-18 已满 1 周无 regression 报告.handoff §3 + docs/scrollback-search.md §8/§12.F2 列表全清:删 `Scrollback::Disk` variant + `DiskScrollback` 整 impl/Drop/Send/Sync(~280 行)+ 5 个 disk-only unit tests + 1 个 disk soak test;删 `Scrollback::disk()` ctor + `alphabet_sb_disk` test helper;删 `MARSPOT_FILE_SCROLLBACK` 检查在 `file_scrollback_session_id`(简化为只读 `MARSPOT_SESSION_ID`)+ 测试 env 操作(scrollback.rs / scrollback_display.rs);删 `MARSPOT_DISK_SCROLLBACK` env gate + `disk_scrollback_enabled` fn + `OnceLock<bool>` 缓存;删 `fallback_disk_or_memory` fn + `DISK_SCROLLBACK_RAM_LINES` / `DISK_SCROLLBACK_PAGES` 常量,重命名残留 `RAM_LINES` → `FILE_SCROLLBACK_RAM_LINES`;删 `Scrollback::evict_disk_pages_for_bench` + `Grid::evict_disk_scrollback_pages_for_bench` + main.rs 2 处 bench 调用(scroll cold 路径 Memory variant no-op,bench CLI surface 保留 `--bench scroll-cold` 兼容);删 `search_enabled()` kill-switch + 2 `handle_cmd_f` / `handle_search_key` 内 gate.Scrollback enum 现在只 Memory + File 两个 variant.624/624 lib tests PASS(去掉 5 disk + 1 soak = 6 fewer than 631 baseline,符合预期).Terminal::new 路径:有 `MARSPOT_SESSION_ID` → File;否则 → Memory(mcli / `--snapshot` / lib tests).无新功能,纯 dead-code 清理.前一条 0.10.99 GlyphAtlas code-quality refactor 笔记保留见 git blame. — cherry-pick perf attack 1/2 from worktree `perf-render-p99-attack`.Phase B 实测 mini render p99 delta = 10.4 µs(1.3%),sub-noise(阈值 1.5× variance band ≈ 18 µs),不挂 "perf 胜利" 名义.但 attack 1(`GlyphKey` 5 字段 12B → u64 newtype 8B)和 attack 2(`AtlasEntry.last_used` 每 hit store → 每 8 帧 decay store)都是 mechanically sound code-quality wins:GlyphKey FxHash 退化成单 u64 mul/rotate 比 5-field derive 链便宜,Eq 单 u64 compare;FxHashMap bucket 密度 +33%;LAST_USED_RESOLUTION=8 让 hot cache hit 路径每 ~8/8 个 store 收敛成 1 个,LRU 行为不变(粗化 ~67ms@120Hz 不影响驱逐顺序).631/631 lib tests PASS.cherry-pick 直接走 worktree 原 commit message(62f4207 + 6235507).预防性:此前 2026-06-24T07:49Z 一次 mini bench 看到 render p99 = 2428 µs(1.87× baseline 1301 µs ceiling)是 mini host noise spike,agent 复测 3 trial 中位 801 µs,baseline 一直 PASS by ~40% margin;font v5 Phase 1-10 整套不引入 perf regression.前一条 0.10.98 font v5 Phase 10d 笔记保留,见 git blame. — font v5 Phase 10d Text 真换行 + resize 跟随(参见 commit 3b19426 完整说明) — Text 真换行(soft wrap)+ resize 跟随.`TextLines::Wrap { max }` 之前只占位 paint 当 single 处理;现 layout 阶段加 `wrap_text_greedy(content, max_w, advance)` 按空白 word-break greedy 装行,每行作为 child `LaidOut` (view=Text/lines=Single/Truncate=None) 挂在 wrap parent 下,total_h = lines × line_h_phys.每行宽用 `FontMetricsProvider::advance_phys` 实测,SF Pro 变体 + weight 都跟随真实字形宽度,不是 mono-cell 估算.paint 阶段 `paint_into_inner` 检测 `View::Text + children non-empty` = wrap parent,跳 `paint_atom` self emit 走 children 递归(每个 child 走原 Single-line TextPrim 路径).max=0 = 无上限,max>0 截到 N 行.dev panel showcase 的 title / header / body / hint / body_w / body_off 默认全部 `.wrap(0)`,panel resize 时 break point 跟着 SF Pro 重排;加 P10d phase 一段长段落演示自动断行.过去 10c bug fix 配合后,dev panel 整套 SF Pro 文本第一次真做到"resize-aware".向后兼容:不调 `.wrap()` 的 caller 保持 `TextLines::Single { truncate: End }` default,不进 wrap 分支. — dev panel SF Pro 字形长宽比修复.根因 1:`shape_with` 的 intern fallback callback 用 postscript_name 去重,但 size+weight variants 共享 postscript_name,导致 Bold 24pt 命中已存在的 Bold 40pt entry,Regular 24pt 命中 startup 13pt entry —— shape 后的 sg.font_id 指向错误大小的 CTFont,glyph rasterise 用错 pt_size.fix:closure 内判断 `f.postscript_name() == base_postscript`,匹配时直接返回 caller 已 intern 的 `base_idx`(我们用 unique_key 区分 size+weight),只有真 fallback(CJK/emoji)走 registry intern.根因 2:`rasterise_glyph_natural` 在 Phase 10c 前 bitmap dims + CGContext CTM 都按 pt-space(bbox.size 直接做 px_w/px_h),但 `shape_line` 内 pen 位置 ×2.0 retina scale,bitmap 和 pen 单位错位 —— 旧 chrome SF Pro 13pt 时差距小看着尚可,Phase 10c 出现 24pt/40pt 后 quad 比 advance 小一半,读作 "等距 mono 排版".fix:bitmap dim × 2.0,ctx.scale(2,2),pen 和 bearing 公式同步.dev panel showcase 改回 production 尺寸(TITLE=22/H=15/BODY=13/EMOJI=20)并展开 6 个 Phase + variable weight 100/400/700/900 渐变.snapshot 验证 Same string regular 24pt vs bold 24pt 现在出 proportional advance 且粗细对.
session = "0.10.0"  # F2 cleanup — marspot-term API 收缩(Scrollback enum 只 Memory + File),L3 走 File variant 不变,但通过 marspot-term 升级 0.9.x → 0.10.0 反映 hot path 二进制级简化(无 disk variant 分支 + 无 env gate 检查 + 单 hash key 命中).  # marspot-term 撤 blank-skip 一并进 L3
