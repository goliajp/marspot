//! `View` — declarative UI tree.
//!
//! Implementation lands in subsequent commits.  This stub keeps
//! `pub mod view` resolvable so `theme` + `Ch` can land first
//! without forward-declaration churn.
//!
//! See `docs/ui-system-model.md` §5+ for the full design.
